import React, { Component } from 'react'
import PropTypes from 'prop-types'

export default class firstsss extends Component {
    static propTypes = {
        prop: PropTypes
    }

    render() {
        return (
            <div>
                || Shree Swami Samarth ||
            </div>
        )
    }
}
